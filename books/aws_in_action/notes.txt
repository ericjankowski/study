Helpful commands
================

cat /proc/cpuinfo

free -m

sudo yum install httpd -y
sudo service httpd start

sudo fdisk -l
df -h
sudo dd if=/dev/zero of=/media/ephemeral0/tempfile bs=1M count=1024 conv=fdatasync,notrunc